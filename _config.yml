title: G4DTG
author:
  name: kraymer
  email: kraymer+g4dtg@gmail.com
description: > # this means to ignore newlines until "show_excerpts:"
  Moteur de recherche pour les parties pédagogiques de blitzstream <https://www.twitch.tv/blitzstream>, histoire de rentabiliser mon temps de confinement.

show_excerpts: false # set to true to show excerpts on the homepage

# Minima date format
# refer to https://shopify.github.io/liquid/filters/date/ if you want to customize this
minima:
  date_format: "%b %-d, %Y"

  # generate social links in footer
  social_links:
    twitter: jekyllrb
    github:  jekyll

# If you want to link only specific pages in your header, uncomment
# this and add the path to the pages in order as they should show up
#header_pages:
# - about.md

# Build settings
theme: minima

plugins:
 - jekyll-feed
 - jekyll-seo-tag

algolia:
   application_id: K3412NKPE1
   index_name: dev
   search_only_api_key: 1ea751f5c1ff3b25f9b53ac518666c7e
   settings:
    searchableAttributes:
      - title
      - unordered(content)
      - collection,categories,tags
      - unordered(ouverture)
      - unordered(elo)  


collections:
    youtube:
      output: true
      permalink: /youtube/:path/
